{
  "name": "bakery-shop-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal bakery ordering MVP for Cloudflare Workers + Static Assets + D1",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "aicommits": "^4.2.2",
    "postal-mime": "^2.7.3"
  }
}
